import {describe,expect,it} from "vitest"; import {calculateScore} from "./scoring";
describe("server scoring preview",()=>{it.each([[0,50],[3000,46],[12000,34],[15000,30]])("scores %sms",(ms,score)=>expect(calculateScore(50,ms,true)).toBe(score));it("wrong is zero",()=>expect(calculateScore(200,0,false)).toBe(0));it("boosts correct",()=>expect(calculateScore(50,0,true,true)).toBe(75));it("does not boost wrong",()=>expect(calculateScore(50,0,false,true)).toBe(0));it("stays bounded",()=>{for(let n=-1000;n<20000;n+=173)expect(calculateScore(80,n,true)).toBeGreaterThanOrEqual(48)})});
